import XCTest
@testable import swift_operator_isolation_verify

class swift_operator_isolation_verifyTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_operator_isolation_verify().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
